CREATE TABLE [bc_rest_cus].[CustomColumnItem] (
    [number] NVARCHAR(20) NOT NULL,
    [globalDimension1Code] NVARCHAR(20) NULL,
    CONSTRAINT [pk_bc_rest_cus_CustomColumnItem] PRIMARY KEY (number)
);
